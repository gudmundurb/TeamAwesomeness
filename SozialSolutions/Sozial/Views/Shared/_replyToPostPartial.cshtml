@model Sozial.Models.CommentModel

@{
    var dateAndTime = DateTime.Now;
    int year = dateAndTime.Year;
    int month = dateAndTime.Month;
    int day = dateAndTime.Day;
}

@using (Html.BeginForm("Reply", "Post")) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">

        <div>
            @Html.HiddenFor(model => model.postID, new { @class = "control-label col-md-2" })
            <div>
                @Html.HiddenFor(model => model.postID)
            </div>
        </div>
        <!--
        <div class="form-group">
            @Html.LabelFor(model => model.commentText, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.commentText)
                @Html.ValidationMessageFor(model => model.commentText)
            </div>
        </div>
        

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Reply" class="btn btn-default" />
            </div>
        </div>
    </div>
-->




    <h4 class="title-adjustments">Write a Reply</h4>
    <div class="widget-area pull-left">
        <form>
            <div>
                @Html.EditorFor(model => model.commentText, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.commentText, "", new { @class = "text-danger" })  
            </div>
            <input type="submit" value="Reply to post" class="btn btn-success pull-right" />
        </form>
    </div>

    <!--
    <div class="widget-area comment-section">
        <a href="#" title="Username">@Html.DisplayFor(modelItem => Model.authorID)</a>
        <small class="pull-right">@day/@month/@year</small>
        @Html.DisplayFor(modelItem => Model.commentText)

        <div class="col-md-3 text-center">
            <span class="btn btn-default btn-sm" onclick="location.href='@Url.Action("Edit", "Post", new { id = Model.commentID })'"><i class="glyphicon glyphicon-pencil"></i> Edit</span>
            <span class="btn btn-danger btn-sm" onclick="location.href='@Url.Action("Delete", "Post", new { id = Model.commentID })'"><i class="glyphicon glyphicon-remove"></i> Delete</span>
        </div>
    </div>
        -->
        </div>
}
